const commonDefinitions = {
    Error: {
        type: 'object',
        properties: {
            message: {
                type: 'string',
            },
        },
    },
};
export default commonDefinitions;
